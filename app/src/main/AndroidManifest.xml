<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petfoodshop">

    <!-- Permissions for accessing external storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Permissions for accessing network state and internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetFoodShop">

        <!-- Main Activity (Login Activity) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registration Activity -->
        <activity android:name=".CreateAccountActivity" />

        <!-- Home Page Activity -->
        <activity android:name=".HomePageActivity" />

        <!-- Profile Details Activity -->
        <activity android:name=".ProfileDetailsActivity" />

        <!-- Admin Login Activity -->
        <activity android:name=".AdminLoginActivity" />

        <!-- Admin Dashboard Activity -->
        <activity android:name=".AdminDashboardActivity" />

        <!-- Pet Food Activity -->
        <activity android:name=".PetFoodActivity" />

        <!-- Admin Add Food Activity -->
        <activity android:name=".AdminAddFoodActivity" />

        <activity android:name=".CustomerArticle" />





    </application>

</manifest>
